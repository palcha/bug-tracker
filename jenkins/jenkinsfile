pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello DevOps'
                sh "echo hello jenkins"
                sh 'date'
                sh 'time'
            }
        }
    }
}