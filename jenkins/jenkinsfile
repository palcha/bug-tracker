pipeline {
    agent any

    stages {
        stage('Unit test - backend') {
            agent{
                docker{
                    image 'snakee/golang-junit:1.21'
                    reuseNode true
                }
            }
            steps {
               dir('bugtracker-backend'){
                sh 'GOCACHE=/tmp/go-build go test -v ./...'
               }
            }
        }
    }
}